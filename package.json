{
  "name": "skillsite",
  "version": "1.2.0",
  "private": true,
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test && node --test \"tests/**/*.test.mts\"",
    "storybook": "pnpm --filter @skillsite/ui storybook"
  },
  "devDependencies": {
    "turbo": "^2.11.2"
  }
}
