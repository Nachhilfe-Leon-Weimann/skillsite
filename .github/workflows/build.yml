name: Build

on:
  workflow_dispatch:
    inputs:
      push:
        description: "Push the built image to GHCR"
        required: true
        default: false
        type: boolean
      source_ref:
        description: "Git ref or SHA to build"
        required: false
        default: ""
        type: string
      version:
        description: "Version tag to apply without leading v"
        required: false
        default: ""
        type: string
      tag_latest:
        description: "Also tag the image as latest"
        required: true
        default: false
        type: boolean
      platforms:
        description: "Target platforms for pushed images"
        required: false
        default: "linux/amd64,linux/arm64"
        type: string
  workflow_call:
    inputs:
      push:
        required: false
        default: false
        type: boolean
      source_ref:
        required: false
        default: ""
        type: string
      version:
        required: false
        default: ""
        type: string
      tag_latest:
        required: false
        default: false
        type: boolean
      platforms:
        required: false
        default: "linux/amd64,linux/arm64"
        type: string

permissions:
  contents: read
  packages: write

concurrency:
  group: ${{ github.workflow }}-image-${{ github.ref }}
  cancel-in-progress: false

jobs:
  image:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.source_ref || github.sha }}

      - name: Resolve image tags
        id: image
        env:
          IMAGE_VERSION: ${{ inputs.version }}
          IMAGE_TAG_LATEST: ${{ inputs.tag_latest }}
          IMAGE_PLATFORMS: ${{ inputs.platforms }}
        shell: bash
        run: |
          repository="${GITHUB_REPOSITORY,,}"
          image="ghcr.io/${repository}"
          source_sha="$(git rev-parse HEAD)"
          short_sha="${source_sha:0:12}"

          {
            printf 'image=%s\n' "${image}"
            printf 'source_sha=%s\n' "${source_sha}"
            printf 'primary_tag=%s\n' "${image}:sha-${short_sha}"
            printf 'platforms=%s\n' "${IMAGE_PLATFORMS}"
            if [[ -n "${IMAGE_VERSION}" ]]; then
              printf 'version_tag=%s\n' "${image}:v${IMAGE_VERSION}"
            fi
            printf 'tags<<EOF\n'
            printf '%s\n' "${image}:sha-${short_sha}"
            if [[ -n "${IMAGE_VERSION}" ]]; then
              printf '%s\n' "${image}:v${IMAGE_VERSION}"
            fi
            if [[ "${IMAGE_TAG_LATEST}" == "true" ]]; then
              printf '%s\n' "${image}:latest"
            fi
            printf 'EOF\n'
          } >> "${GITHUB_OUTPUT}"

      - name: Set up QEMU
        if: inputs.push && contains(inputs.platforms, 'arm')
        uses: docker/setup-qemu-action@1f40c72289eff860ee54a304f1438e3cff362e0a # v4.3.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

      - name: Log in to GHCR
        if: inputs.push
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      # No build secret and no build argument: every dependency is public, and everything the image
      # needs - including the version in the root package.json - is in the build context.
      - name: Build and push image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: Dockerfile
          platforms: ${{ inputs.push && inputs.platforms || 'linux/amd64' }}
          push: ${{ inputs.push }}
          tags: ${{ steps.image.outputs.tags }}
          provenance: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
